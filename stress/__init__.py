"""Local stress harness: corpus, Audio API stub, seeder, monitor, chaos.

Not part of the shipped SDK -- none of this is packaged into an image that runs
in production. It exists to drive the real thing hard enough locally that its
failure modes show up here rather than there.
"""
