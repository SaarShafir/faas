apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      # Matches prometheus.yml's scrape_interval. Getting this wrong makes
      # rate() windows silently too short and every graph go sawtoothed.
      timeInterval: 5s
