function_id: silence_ratio
function_version: "1.0.0"
image: registry/faas-silence-ratio:1.0.0
resources:
  cpu: 1
  memory: 512Mi
# One reshape and one mean over a 5-minute mono file. Nothing here needs depth.
in_flight: 2
per_file_timeout_seconds: 30
retry_budget: 3
dlq_topic: faas.dlq.silence_ratio
